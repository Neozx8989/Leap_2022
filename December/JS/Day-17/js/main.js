function pow(number, exponent) {
    let multiply = 1;
    for(let i = 1; i <= exponent; i++) {
    }
    return multiply;
}
let result = pow(2,3);
console.log(result);
console.log(pow(2,4));
console.timeLog(pow(5, 2));

console.log(Math.pow(4, 3));
console.log(pow(4,3));
